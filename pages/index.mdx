---
type: page
title: Forge笔记
date: 2024-01-26
description: 记录在熔炉里打磨的思考
tag: blog,tech,前端
author: 博主
---

import { getPagesUnderRoute } from 'nextra/context'
import Link from 'next/link'

export const getStaticProps = () => {
  return {
    props: {
      posts: getPagesUnderRoute('/posts')
        .map((page) => ({
          ...page,
          date: page.frontMatter?.date || page.meta?.date
        }))
        .filter(page => page.date && page.route !== '/posts')
        .sort((a, b) => new Date(b.date) - new Date(a.date))
        .slice(0, 5)
    }
  }
}



# 🚀 欢迎来到Forge笔记

记录在熔炉里打磨的思考。

## 📚 最新文章 (共 {props.posts.length} 篇精选)

<div style={{ marginBottom: '2rem' }}>
{props.posts.map((post, index) => (
  <article key={post.route} style={{
    padding: '1.5rem',
    border: '1px solid #e5e7eb',
    borderRadius: '8px',
    background: '#fafafa',
    marginBottom: '1rem',
    transition: 'all 0.2s ease'
  }}>
    <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'flex-start', marginBottom: '0.75rem' }}>
      <h3 style={{ margin: 0, fontSize: '1.25rem', fontWeight: '600' }}>
        <Link href={post.route} style={{ textDecoration: 'none', color: '#1f2937' }}>
          {post.meta?.title || post.frontMatter?.title || post.name}
        </Link>
      </h3>
      <span style={{ 
        fontSize: '0.875rem', 
        color: '#6b7280',
        backgroundColor: '#f3f4f6',
        padding: '0.25rem 0.5rem',
        borderRadius: '4px'
      }}>
        #{index + 1}
      </span>
    </div>
    
    <p style={{ 
      margin: '0 0 1rem 0', 
      color: '#6b7280', 
      fontSize: '0.875rem',
      lineHeight: '1.5'
    }}>
      {post.frontMatter?.description || '暂无描述'}
    </p>
    
    <div style={{ 
      display: 'flex', 
      justifyContent: 'space-between', 
      alignItems: 'center',
      fontSize: '0.75rem',
      color: '#9ca3af'
    }}>
      <div style={{ display: 'flex', gap: '1rem', alignItems: 'center' }}>
        {post.date && (
          <span>📅 {new Date(post.date).toISOString().split('T')[0]}</span>
        )}
        {post.frontMatter?.author && (
          <span>👤 {post.frontMatter.author}</span>
        )}
      </div>
      
      {post.frontMatter?.tag && (
        <div style={{ display: 'flex', gap: '0.5rem' }}>
          {post.frontMatter.tag.split(',').slice(0, 2).map((tag, i) => (
            <span key={i} style={{
              backgroundColor: '#dbeafe',
              color: '#1e40af',
              padding: '0.125rem 0.5rem',
              borderRadius: '12px',
              fontSize: '0.7rem'
            }}>
              {tag.trim()}
            </span>
          ))}
        </div>
      )}
    </div>
  </article>
))}


</div>

---

<div style={{ textAlign: 'center', marginTop: '3rem', padding: '2rem', background: '#f0f9ff', borderRadius: '12px', border: '2px solid #bae6fd' }}>
  <h2 style={{ margin: '0 0 1rem 0', color: '#0369a1', fontSize: '1.5rem' }}>👋 欢迎来到我的技术世界</h2>
  <div style={{ margin: '0 0 1.5rem 0', color: '#0c4a6e', lineHeight: '1.6', fontSize: '1.1rem' }}>
    这里记录着我在技术路上的思考与实践
  </div>
  <div style={{ display: 'flex', justifyContent: 'center', gap: '1rem', flexWrap: 'wrap' }}>
    <Link href="/about" style={{
      display: 'inline-block',
      padding: '0.75rem 1.5rem',
      background: '#0369a1',
      color: '#ffffff',
      textDecoration: 'none',
      borderRadius: '8px',
      fontSize: '0.95rem',
      fontWeight: '600',
      transition: 'all 0.2s ease'
    }}>
      👨‍💻 了解更多关于我
    </Link>
    <Link href="/posts" style={{
      display: 'inline-block',
      padding: '0.75rem 1.5rem',
      background: '#ffffff',
      color: '#0369a1',
      textDecoration: 'none',
      borderRadius: '8px',
      fontSize: '0.95rem',
      fontWeight: '600',
      border: '2px solid #0369a1',
      transition: 'all 0.2s ease'
    }}>
      📚 浏览所有文章
    </Link>
  </div>
</div>

---

<div style={{ textAlign: 'center', color: '#6b7280', fontSize: '0.875rem' }}>
  💡 感谢您的访问，希望这些文章对您有所帮助！
</div>